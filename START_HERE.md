# はじめに（このフォルダで最初にやること）

このフォルダが、あなたの秘書AI が住む「家」になります。

## 0. まず、フォルダの置き場所を決める（先にやってください）

**あとから動かすと、許可の設定をやり直すことになります。** 開く前に置き場所を決めてください。

| お使いの PC | 置き場所 |
|---|---|
| **Windows** | **ドキュメント**（`ドキュメント\AI秘書`） |
| **Mac** | **デスクトップ**（`デスクトップ/AI秘書`） |

Windows でドキュメントを勧めるのは、OneDrive で同期されるので、別の PC を使うときに引き継げるからです。

## 1. 開いて、1 行貼る

置いたフォルダを **Claude のデスクトップアプリで開いて**、下の 1 行をそのまま貼って送信してください。

```
setup/onboarding.md を読んで、その手順どおりに私を案内してください。
```

途中で「このフォルダを信頼しますか」と聞かれたら **はい**。
ここで「いいえ」にすると、安全の仕掛けも自動の仕掛けも一切効きません。

あとは秘書AI が 1 つずつ質問してくれます。所要 15〜20 分。
**途中で止まっても大丈夫です。** 同じ 1 行をもう一度貼れば、終わったところの次から再開します
（どこまで進んだかは秘書AI が記録しています）。

---

## 何が起きるか（先に全部お見せします）

| | 聞かれること | 何のため |
|---|---|---|
| 1 | 秘書AI の呼び名 | 呼びかけを決める |
| 2 | あなたの仕事を一言で | 判断の軸を持たせる |
| 3 | **絶対に渡さない情報 3 つ**（赤レーン） | **これがセキュリティの設定です**。3 回聞かれます |
| 4 | 秘書AI に真っ先にやってほしい仕事 | 最初の一手を決める |
| 5 | よく使う業界用語 5 つ | 用語で誤解しないため |

**3 番がセキュリティです。** 「渡したくない情報」という聞き方をしますが、
ここで答えたものは、この先ずっと秘書AI が触りません。思いつかなければ「なし」でも進みます。

この 5 つが終わると、その場から使える状態になります（調べもの・壁打ちの 2 つの道具も一緒に繋がります）。

### 設定が終わったら、1 回だけ試してほしいこと

```
.env を読んでみて
```

**読めずに止まります。** これは秘書AI が「読まないようにしている」のではなく、
**機械的に実行できない**ように最初から設定してあるからです。鍵やパスワードの類は同じように守られます。

---

## 講師に進み具合を伝えるとき

```
家の状態を教えて
```

件数だけの表が出ます。**中身（渡さない情報・業界用語・人の名前）は入りません**ので、
そのまま講師に送って大丈夫です。

## うまく進まないときは（そのまま貼ってください）

**①「それはできません」と言われた**

```
できる方法を考えて
```

秘書AI は、少し考えれば届く範囲でも「できません」と答えることがあります。
**一度で引き下がらないでください。** これが一番よく効く一言です。

**② 画面のどこを触ればいいか分からない**

画面をスクリーンショットして、そのまま貼り付けて聞いてください。

```
（スクショを貼って）どこを操作すればいいですか？
```

パソコンの設定（既定のブラウザなど）の困りごとも、そのまま聞いて構いません。

**③ 安全の設定だけ、確実にやる**（これが最優先。ここだけは必ず終わらせてください）

```
setup/onboarding.md の Step 3 だけを、今から実行してください。他のステップには進まないでください。私に 1 問ずつ聞いて、governance/security.md を作り、最後に差分を見せて承認を取ってください。
```

**④ 道具を繋ぐところだけ、確実にやる**

```
setup/onboarding.md の Step 6 だけを、今から実行してください。私への質問はせず、変更の差分だけ見せて承認を取ってください。
```

**⑤ どこまで終わったか分からなくなった**

```
setup/_progress.txt を読んで、どこまで終わったか教えてください。そのうえで、次のステップだけを実行してください。
```

---

## 事故を防ぐ 3 つの言葉（覚えておくと安全です）

大きな作業（ファイルの整理・削除・一括の変更）を頼むときは、この 3 つを足してください。

> **まず計画を見せて。承認してから進めて。削除はしないで。**

これを付けないと、秘書AI が自分の判断でどんどん進めてしまうことがあります。
最初のうちは、**本番のフォルダではなくテスト用のフォルダで練習**してください。
そして、**出てきたものの最終チェックは必ずあなたがしてください。**

---

## 必要なもの

- **Claude のデスクトップアプリ**（Pro プラン）
- **アプリを動かすための道具**（アプリが求めてきます。指示どおり入れてください）
  - **Windows** → **Git for Windows**（青い画面のインストーラーが出ます）
  - **Mac** → **コマンドライン・デベロッパ・ツール**（「インストール」を押すだけ）
- Node.js と Python は要りません（Windows は PowerShell、Mac は sh で動くように作ってあります）

> ※ **Git（ギット）と GitHub（ギットハブ）は別物**です。
> 上で入れるのは **Git**（アプリを動かす道具）。
> **GitHub** は会員の棚を受け取るためのアカウントで、こちらはインストールするものではありません。

## この家に最初から入っているもの

| | 中身 |
|---|---|
| `setup/` | 導入の台本と**機械の仕掛け**（秘書AI が読みます。あなたは読まなくて大丈夫です） |
| `saho/` | **作法の棚**（13 枚）。場面が来たら秘書AI が自動で思い出します |
| `skills/research.md` | 調べもののやり方（出典つきで保存します） |
| `roles/consultation/profile.md` | 壁打ち相手としての振る舞い |
| `context/people/` `context/projects/` | **人と案件のメモが自動でたまる場所**。次に名前を出すだけで思い出します |
| `brain/trigger_map.md` | 「この言葉が出たらここを見る」の索引。使いながら 1 行ずつ足します |
| `setup/review.md` | **月 1 回の棚卸し**（「棚卸しをして」と言うだけ） |
| `setup/report.md` | **家の状態を件数だけで出す**（「家の状態を教えて」と言うだけ） |
| `setup/shelf_import.md` | 会員の棚を受け取る手順（会員の方のみ） |
| `.claude/settings.json` | **機械的な禁止**（鍵ファイルを読む・設定を自分で書き換える・外部送信コマンド・一括削除・公開への push）。お願いではなく、実行できない設定です |

### 勝手に動く 3 つの仕掛け（あなたは何もしなくていい）

1. **作法の照合** — その場面に効く一手を、秘書AI が言われる前に思い出す
2. **家の地図** — 人や案件の名前を出すと、過去のメモの場所が秘書AI に届く
3. **空振りの記録** — どれにも当たらなかった場面を貯めて、棚卸しの材料にする

1 週間ほど使った後に、**運用ルール**（作業の台帳・記憶の残し方）が別途届きます。
